import styled from "styled-components";
import { ButtonStyle } from "../Button/style";

export const CopyButtonStyle = styled(ButtonStyle)`
    @media (max-width: 600px) {
        min-width: 100%;
    }
`;